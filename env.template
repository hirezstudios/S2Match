# S2Match SDK - Environment Configuration
# Rename this file to .env and fill in the values

# ------------------------------------------------------------------------------
# Required: Environment API Credentials
# ------------------------------------------------------------------------------
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
RH_BASE_URL=your_rallyhere_base_url_here

# ------------------------------------------------------------------------------
# Optional: SDK Configuration
# ------------------------------------------------------------------------------
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable/disable response caching (true, false)
CACHE_ENABLED=true

# Delay in seconds between API calls (helps with rate limiting)
# Examples: 0 (no delay), 0.5 (500ms delay), 1.0 (1 second delay)
RATE_LIMIT_DELAY=0

# ------------------------------------------------------------------------------
# Optional: Application Settings
# ------------------------------------------------------------------------------
# Only needed if using the example Flask application (app.py)
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=1 